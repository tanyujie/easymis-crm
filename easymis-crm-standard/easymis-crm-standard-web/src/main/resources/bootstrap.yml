server:
  port: 50102
spring:
  application:
    name: easymis-crm-standard-web
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
        addresses: amqp://localhost:5672
        username: alan
        password: alan

encrypt:
  failOnError: false

org:
  easymis:
    leads:
      name: test
      title: test